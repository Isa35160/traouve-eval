{# templates/category/dropdown.html.twig #}

    {% for category in categories %}
        <li>
            <a class="text-muted" href="{{ path('category_show', {'id' : category.id}) }}">
                {{ category.label }}
            </a>
        </li>
    {% endfor %}
